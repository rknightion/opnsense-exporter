apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opnsense2otel.fullname" . }}-test-version"
  labels:
    {{- include "opnsense2otel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  automountServiceAccountToken: false
  restartPolicy: Never
  securityContext:
    runAsNonRoot: true
    runAsUser: 65532
    runAsGroup: 65532
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: version
      image: {{ include "opnsense2otel.image" . | quote }}
      imagePullPolicy: {{ .Values.image.pullPolicy }}
      args:
        - "--version"
      securityContext:
        {{- include "opnsense2otel.securityContext" . | nindent 8 }}
