version: 2

# Pinned rather than derived. GoReleaser defaults project_name to the repository
# directory name, and scripts/docgen reads this key to build the release-asset names
# it documents — so without it, running `make docs` from a git worktree derives the
# asset name from the worktree directory and hard-fails. A no-op for real releases:
# CI checks out into `opnsense2otel`, which is the value derived today.
project_name: opnsense2otel

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
      - freebsd
      - openbsd
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    tags:
      - osusergo
      - netgo
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}

archives:
  - formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

# Per-archive SBOMs (syft must be on PATH — installed by the binaries reusable's pre-cmd).
sboms:
  - id: archives
    artifacts: archive

# Keyless cosign signature over the checksums file (GitHub OIDC). Requires id-token: write
# and cosign on PATH (the binaries reusable's install-cosign: true). The checksums file
# cryptographically commits to every archive, so one signature covers the whole release.
signs:
  - cmd: cosign
    signature: "${artifact}.sigstore.json"
    args:
      - sign-blob
      - "--bundle=${signature}"
      - "${artifact}"
      - "--yes"
    artifacts: checksum
    output: true

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  disable: true

# Without this, an RC tag (vX.Y.Z-rc.N) would publish as a full GitHub release and
# steal "Latest" from the most recent stable release.
release:
  prerelease: auto
