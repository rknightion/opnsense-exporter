# Pinned Python deps for the api-contract parser (opnsense/docs lib), so the CI job
# executes a known set of packages rather than whatever PyPI ships that day (#130).
# Renovate's pip_requirements manager bumps these automatically.
phply==1.2.6
jinja2==3.1.6
pydantic==2.13.5
# Test runner for extract_test.py, wired into the api-contract CI job (#136).
pytest==9.1.1
