// opnsense-exporter operator console — client runtime.
// Task 5 ships a placeholder; live polling, Run-Now wiring, and filter/sort are
// completed in Task 9. Everything must existence-guard its targets so pages
// without a given element are unaffected.
console.log('opnsense ui');
